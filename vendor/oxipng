# oxipng is not in apt — install the latest release .deb from GitHub
url=$(curl -fsSL https://api.github.com/repos/oxipng/oxipng/releases/latest | grep -o 'https://[^"]*_amd64\.deb' | head -1)
curl -fsSL "$url" -o /tmp/oxipng.deb && sudo apt install -y /tmp/oxipng.deb && rm -f /tmp/oxipng.deb
